const math = require("./math.js");
/**
 * All the required modules will add in the childern array of the module object.
 * isPreloading - a setter function. If there is a need to preload a module, we can do : node --require <filename> <require_filename> | The filename where the module is required. By default it's false.
 */

// console.log(module);
