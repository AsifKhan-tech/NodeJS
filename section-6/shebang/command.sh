# A shebang, hashbang, pound-bang, or sharp-bang is the character sequence at the absolute beginning of a script. It acts as an interpreter directive, telling the operating system's kernel exactly which program to use to parse and execute the rest of the file.

# npm and npx both use the shebang

#!/bin/env bash