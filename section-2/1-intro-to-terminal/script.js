"use strict";

console.log("Start backend journey!");

/**
 * create variables in terminal like this- name="Asif"
 * Access these variables in terminal like this- $name, return the value of name
 * we can also print these variables in terminal like this- echo $name
 * run the script / (.sh) file:- path<filename.sh>
 *** It will not directly run in linux system, you will have to give permission(chmod +x filename.sh)

 * echo - can print the content in terminal like (console.log("content"))
 * pwd - print the working directory from the root
 * whoami - give the current user of the system
 * cd - change directory
 * ls - list the content

 *
 */

/**
 * / :- root directory(top level / absoloute top) Starting point of the entire file system. Everything lives under this.
 * Access - Only root user / administrator has full permission to change files here.
 * Purpose - Hold all system files, programms, settings and other directories like /bin, /etc, /home
 */

/**
 * ~ :- home directory - Your personal workspace where you keep your own files, downloads, and custom settings.
 * Access - Own your home directory & have full control over the files inside it.
 * purpose - Keep your personal documents safe away from vital system files.
 */
