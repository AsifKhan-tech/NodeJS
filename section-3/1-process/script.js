const a = process;
console.log("PROCESS: ", a);

/**
 * lscpu - return the system configuration
 * nproc - return the logical cores / threads
 * process can create / start a thread and also use it too
 * */
